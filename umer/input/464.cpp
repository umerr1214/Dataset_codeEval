#include <iostream>
using namespace std;

double calculateVolume(double length, double width, double height) {
    // Logical Error: Adds the dimensions instead of multiplying them.
    return length + width + height;
}